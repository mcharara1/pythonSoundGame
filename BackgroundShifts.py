import turtle
import Background


private void timer1_Tick(object sender, EventArgs e) # type: ignore
{Background.Left += 5; new_func() Move right by 5 pixels # type: ignore
    if (Background.Left > this.Width) // turtle.reset position if it goes off-screen # type: ignore
    {Background.Left = -bground.py.Width;}}